/**
 *
 * @param {*} arr
 * 插入排序前提： 当前元素前面的序列是有序的
 */
function insert(arr) {
  // 准备一个新数组，用来存储抓到手里的牌，开始先抓一张牌过来
  let handle = [];
  handle.push(arr[0]);
  for (let i = 1; i < ary.length; i++) {
    // a 是新抓的牌
    let a = ary[i];
  }
}

function insertSort(arr) {
  const len = arr.length;
  let temp;
  // for()
}
